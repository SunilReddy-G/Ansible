- name: variable preference
  hosts: local
  connection: local
  vars:
    NAME: "Ansible from FILE"
  vars_files:
    -name: NAME
    prompt: please enter name
    private: false
    tasks:
    - name: print name
      vars:
        NAME: "Ansible from TASK"
      ansible.builtin.debug:
        msg: "Hi {{ NAME }}"